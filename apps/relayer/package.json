{
  "name": "@velo/relayer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain relayer: watches Soroban atomic-swap release() events and claims the counterpart HTLC on an EVM chain.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "@velo/shared": "*",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "express": "^5.2.1",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.19.43",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
